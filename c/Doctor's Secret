#include <stdio.h>

int main(){
	int l,p;
	scanf("%d %d ", &l,&p);
	if(l<=23 &&p>=500&&p<=1000)
	{
	    printf("Take Medicine");
	}// Reading input from STDIN
	else
	{
	    printf("Don't take Medicine");
	}
	return 0;
}
